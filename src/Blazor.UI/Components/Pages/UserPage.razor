@page "/User"
@using Blazor.UI.Components.PageComponents
@using Blazor.UI.Components.UserComponents
@attribute [StreamRendering]
@rendermode InteractiveServer

<h2>User Page</h2>

<NavigateHome />

@if (DataSource.Loading)
{
    <LoadingComponent />
}
else
{
    <UserGrid DataSet="@DataSource.DataSet"/>
    <br />
}